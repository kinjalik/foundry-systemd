galaxy_info:
  role_name: foundry_systemd
  author: Albert Akmukhametov
  description: Foundry as a Systemd unit with separate user
  license: "license (MIT)"

  min_ansible_version: "2.10"

  galaxy_tags:
    - systemd
    - foundry

  platforms:
    - name: Ubuntu
      versions:
        - all

dependencies:
  - role: geerlingguy.nodejs
    vars:
      nodejs_version: '18.x'
  - role: andrewrothstein.unarchive-deps
